---
title: "Header Component"
description: "Add the Header components in your Next.js project"
---

### Header Component

<Frame>
  <img src="/images/adminheader.png" style={{ borderRadius: '0.5rem' }} />
</Frame>

Here's how to use the  component:

```jsx
import Image from "next/image";
import Link from "next/link";
import admin_img from "@/images/admin_img.webp";

interface AdminHeaderProps {
  style?: string;
}

const AdminHeader = (props: AdminHeaderProps) => {
  return (
    <header
      style={{ width: props.style }}
      className={`absolute top-0 left-0 w-full bg-neutral-950 border-b border-neutral-700 text-white flex items-center justify-between px-4 py-3 shadow-md z-10`}
    >
      <div className="flex items-center">
        <div className="text-xl font-bold">FitnEarn</div>
      </div>
      <Link href="/profile" className="ml-auto">
        <div className="flex items-center gap-2">
          <Image
            width={30}
            height={30}
            className="h-8 w-8 rounded-full object-cover"
            loading="eager"
            alt="Profile"
            src={admin_img}
          />
          <span className="font-medium">Aniruddha Teke</span>
        </div>
      </Link>
    </header>
  );
};

export default AdminHeader;


```
