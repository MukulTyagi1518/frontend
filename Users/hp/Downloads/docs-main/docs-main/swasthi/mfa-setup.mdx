---
title: "Multiple Factor Authentication Setup Component"
description: "Add theMultiple Factor Authentication  components in your Next.js project"
---

### Multiple Factor Component 

<Frame>
  <img src="/images/mfa-setup.png" style={{ borderRadius: '0.5rem' }} />
</Frame>

Here's how to use the  component:

```jsx
 "use client";


import Image from "next/image";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

export default function MFASetup() {
  return (
    <div className="min-h-screen bg-[#121212] flex items-center justify-center p-4">
      <div className="w-full max-w-md bg-[#1E1E1E] rounded-lg p-8 shadow-lg relative">
        {/* Logo and Title */}
        <div className="text-center space-y-4 mb-8">
          <div className="mx-auto w-16 h-16 relative">
            <div className="flex flex-col items-center space-y-2">
                      <Image
                        src="/logo.svg"
                        alt="Swasthi Logo"
                        width={64}
                        height={64}
                        className="rounded-full"
                      />
                     
                    </div>
          </div>
          <h1 className="text-white text-xl font-semibold">Swasthi</h1>
        </div>

        {/* Steps Section */}
        <div className="relative space-y-10">
          {/* Step 1 */}
          <div className="flex gap-4 relative items-start">
            {/* Step Number with Fully Connected Line */}
            <div className="relative z-10 flex flex-col items-center">
              <div className="w-7 h-7 rounded-full bg-black border-2 border-gray-500 flex items-center justify-center">
                <span className="text-white text-sm font-medium">1</span>
              </div>
              <div className="w-[2px] h-16 border-l-2 border-dashed border-gray-500"></div> {/* Full Line to 2 */}
            </div>
            <div className="space-y-2 text-gray-300 text-sm">
              <p>
                Install a compatible application such as Google Authenticator, Duo Mobile, or Authy app on your mobile
                device or computer.
              </p>
              <button className="text-gray-400 hover:text-gray-200 underline text-xs">
                See a list of compatible applications
              </button>
            </div>
          </div>

          {/* Step 2 */}
          <div className="flex gap-4 relative items-start">
            <div className="relative z-10 flex flex-col items-center">
              <div className="w-7 h-7 rounded-full bg-black border-2 border-gray-500 flex items-center justify-center">
                <span className="text-white text-sm font-medium">2</span>
              </div>
              <div className="w-[2px] h-16 border-l-2 border-dashed border-gray-500"></div> {/* Full Line to 3 */}
            </div>
            <div className="flex gap-4 w-full">
              <div className="w-24 h-24 bg-white p-1 flex-shrink-0">
                <Image
                  src="/placeholder.svg?height=100&width=100"
                  alt="QR Code"
                  width={100}
                  height={100}
                  className="w-full h-full"
                />
              </div>
              <div className="text-gray-300 text-sm flex-1 text-justify">
                <p>
                  Open your authenticator app, choose Show QR Code on this page, then use the app to scan the code.
                  Alternatively, you can type a secret key.
                </p>
                <button className="text-gray-400 hover:text-gray-200 underline text-xs">
                  Show secret key
                </button>
              </div>
            </div>
          </div>

          {/* Step 3 */}
          <div className="flex gap-4 relative items-start">
            <div className="relative z-10 flex flex-col items-center">
              <div className="w-7 h-7 rounded-full bg-black border-2 border-gray-500 flex items-center justify-center">
                <span className="text-white text-sm font-medium">3</span>
              </div>
            </div>
            <div className="space-y-2 w-full">
              <p className="text-white text-sm">Type MFA code below</p>
              <p className="text-gray-400 text-xs">Enter a code from your virtual app below</p>
              <Input
                type="text"
                placeholder="MFA Code"
                className="bg-[#2A2A2A] border-gray-600 text-white mt-2 h-10 text-sm px-3"
              />
            </div>
          </div>
        </div>

        {/* Buttons */}
        <div className="flex gap-4 pt-8">
          <Button
            variant="outline"
            className="flex-1 bg-transparent text-white border-gray-600 hover:bg-gray-800 hover:text-white h-10 text-sm"
          >
            Cancel
          </Button>
          <Button className="flex-1 bg-white text-black hover:bg-gray-300 h-10 text-sm">
            Add MFA
          </Button>
        </div>
      </div>
    </div>
  );
}





```
